<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Операция №: {{ transaction.id }} - {{ transaction.status.name }}</title>
</head>
<body>
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <ul>
        <li>Операция №: {{ transaction.id }}</li>
        {% if transaction.type %}
        <li>Тип операции: {{ transaction.type }}</li>
        {% endif %}
        <li>Сумма: {{ transaction.sum }}</li>
        <li>{{ direction }}</li>
        <li>Валюта: {{ transaction.currency.name }}</li>
        <li>Дата заявки: {{ transaction.createdAt.format('d.m.Y H:i') }}</li>
        <li>Статус операции: {{ transaction.status.name }}</li>
        <li>ID Администратора: {{ adminId }}</li>
    </ul>
    <div>
        С уважением, команда WSI.
    </div>
</div>
</body>
</html>